## 1. Entscheidungsvorbereitung (Voraussetzung für Freigabe)

- [ ] 1.0 Design vorab exakt ausarbeiten (interaktiver Prototyp/Mockup via Claude-Artifacts) auf Basis Referenz-Screenshot + Logo-Farben; abnehmen lassen
- [ ] 1.1 Design-System-Ansatz festlegen (Tokens-Quelle, Komponentenbibliothek, Governance) — siehe `design.md` Decision 1
- [~] 1.1a Farb-Tokens festlegen: **Logo-Farben führend** (`#0e72b5`/`#de313b`/`#97bf0d`/`#666666` aus `ServerColorTheme`) als Primärtoken + Neutralfarben aus dem Screenshot (Navy `#0b1b2c`, Off-White, Creme) → in `frontend/src/styles/tokens.css` implementiert (hell/dunkel). Offen: finale Tints/Shades + WCAG-Kontrastprüfung; Layout NICHT aus dem Mockup ableiten
- [ ] 1.1a-2 Typografie festlegen: self-hostbare Open-Source-Grotesk-Sans (im WAR gebündelt); exakte `font-family` aus dem CSS der Live-Website bestätigen
- [ ] 1.1b Responsive Breakpoints + adaptive Layout-Muster definieren (Desktop/Tablet/Smartphone, Tabelle→Karte) — siehe `design.md` Decision 3a
- [x] 1.2 SPA-Framework per gewichteter Bewertung entscheiden → **Angular** (siehe `design.md` Decision 2b)
- [ ] 1.2a Bestätigungs-Spike Angular: ein Modul (Fallliste + Falldetail + Datentabelle) mit Logo-Tokens; Entwickler bewerten die **Lesbarkeit/Verständlichkeit** des von Claude generierten Codes
- [ ] 1.2d Frontend-Coding-Standards für AI-Generierung festlegen: ESLint + Prettier, CLI-Schematics, Signals-first (RxJS nur wo nötig), Frontend-Styleguide/AGENTS.md für den Assistenten
- [ ] 1.2b Kostenfreie Komponentenbasis festlegen: Angular Material + CDK (MIT) und/oder PrimeNG (MIT); Grid nur AG Grid Community (MIT), Enterprise/Kendo/Syncfusion/DevExtreme/Vaadin Pro ausschließen
- [ ] 1.2c Custom-Component-Machbarkeit via CDK verifizieren (Beispiel: eigene Komponente ohne Fertigbaustein) und `ng update`-Migrationspfad dokumentieren
- [x] 1.3 Deployment-Modell entschieden: **eigenständiger WAR `j-lawyer-web`, separat auf derselben WildFly** (siehe `design.md` Decision 2a)
- [ ] 1.4 Auth-Modell festlegen (session-/tokenbasiert) und mit `add-two-factor-auth` abstimmen
- [ ] 1.5 Push-Mechanismus wählen (WebSocket/SSE/Polling) für Messenger, Erinnerungen, Status-Badges
- [ ] 1.6 Office-Editing-Strategie entscheiden (WOPI via Collabora/OnlyOffice als Ziel; Download/Upload als MVP-Fallback)
- [x] 1.7 REST-Gap-Analyse: 24 EJB-Remote-Interfaces vs. REST v1–v8; fehlende Endpunkte je Modul dokumentiert → siehe `gap-analysis.md`
- [ ] 1.8 Offene Fragen aus `design.md` beantworten und Entscheidungen dort festhalten

## 2. Phase 0 — Fundament

- [~] 2.1 Design-System-Paket / App-Shell — **responsive Shell implementiert & Build verifiziert**: `ShellComponent` (Grid Header/Modulleiste/Content/Bottom-Nav, Breakpoints Desktop→Icon-Rail→Bottom-Nav), `AppHeaderComponent`, `ModuleNavComponent` (Router-aktiv), `BottomNavComponent`, `ThemeService` (hell/dunkel via `data-theme`), CSP-sichere `IconComponent` (`@switch`, kein innerHTML), Routen aus Modul-Liste generiert, `ModulePlaceholderComponent`. Offen: Fehler-/Ladezustände, Komponentenkatalog (Storybook), echte Modulinhalte
- [~] 2.2 Neues Maven-Modul `j-lawyer-web` (eigenständiger WAR) — **erstellt & Build verifiziert**: Angular-19-Projekt in `frontend/` (standalone, Signals-first), `pom.xml` (`frontend-maven-plugin` + `maven-war-plugin`), `web.xml` SPA-Fallback, opt-in Profil `-Pweb`. `npm install` (848 Pakete, `package-lock.json` committet), `ng build` und `mvn -Pweb ... install` erzeugen `target/j-lawyer-web.war` erfolgreich; `deploy.sh` kopiert das WAR (wenn gebaut) nach WildFly → erreichbar unter `/j-lawyer-web`. Offen: Offline-Vendoring (`vendor-npm/` + `npm ci --offline`)
- [~] 2.2a Laufzeit-Härtung — **CSP-Baseline gesetzt**: alle Assets self-hosten (keine CDNs), CSP-`<meta>` in `index.html` (`default-src 'self'`; `script-src 'self'`). Offen: autoritative CSP-/Security-Header auf WildFly/Undertow (inkl. `frame-ancestors`), Nonce für Component-Styles (`ngCspNonce`), Trusted Types
- [x] 2.2b Build-Härtung — Guardrails-Config & Policy etabliert (vor dem Scaffold): `j-lawyer-web/.npmrc` (`ignore-scripts`, `save-exact`, `package-lock`, `engine-strict`, `audit=false`), `.gitignore`, `SUPPLY-CHAIN.md`, `scripts/seed-npm-cache.sh` (Analog zu `maven-repo/`). `esbuild`-`postinstall` verifiziert: Build gelingt mit `--ignore-scripts` (esbuild-Binary via optionalDependencies, kein Skript nötig). Offen: Offline-Vendoring (`vendor-npm/`), `npm audit`-CI-Gate + Dependabot scharf schalten
- [x] 2.2d i18n-Fundament (Transloco) — **implementiert & verifiziert**: DE/EN, Laufzeit-Umschaltung im Header, self-hosted JSON (`public/i18n/*.json`), `core/i18n.ts` als zentrale Sprachliste (neue Sprache = 1 JSON + 1 Eintrag), Persistenz via `localStorage`, Init aus gespeichert>Browser>Default. Alle Shell-Strings über Transloco-Keys. Verifiziert per Headless-Chromium (DE↔EN wechselt Labels/Suche/Überschrift). Offen: `TranslocoTitleStrategy` für übersetzte Seitentitel
- [ ] 2.3 Login-/Auth-Flow implementieren (siehe `design.md` Decision 5 — additiv, kompatibel):
  - [~] 2.3a Server: Elytron-Bearer-Mechanismus (`BEARER_TOKEN` + `token-realm`, RS256) **neben** BASIC — **umgesetzt in `docker/wildfly/standalone.xml`**: `key-store jlawyer-jwt-keystore` (PKCS12), `token-realm jlawyer-jwt-realm` (`sub`-Principal, `roles`-Claim), Realm in `jlawyer-security-domain` (Decoder `from-roles-attribute` → Identität propagiert zur EJB-Ebene), `http-authentication-factory jlawyer-http-authentication` (BASIC+BEARER_TOKEN), Undertow-`application-security-domain` → Factory mit `override-deployment-config`; EJB-Mapping bleibt `security-domain`. `/rest/v8/auth/*` in `web.xml` öffentlich; 401 ohne `WWW-Authenticate: Basic`. Setup dokumentiert in `j-lawyer-io/AUTH-SETUP.md`. **E2E am laufenden WildFly 26.1.3 verifiziert (2026-07-08):** Bearer-Zugriff auf `/v1/cases/list/active` (filtert intern per `getCallerPrincipal()`) → 200, d.h. JWT-Identität propagiert zur EJB-Ebene. *(Option A verworfen — s. Decision 5, revidiert 2026-07-08.)*
  - [~] 2.3b Server: `POST v8/auth/login|refresh|logout` — **implementiert**: `AuthenticationEndpointV8` (+ pojos `LoginRequestV8`/`TokenResponseV8`), RS256-JWT-Access-Token + httpOnly-Refresh-Cookie (`JLAWYER_REFRESH`, SameSite=Lax, Secure wenn HTTPS); Credential-Prüfung via neuer `@PermitAll`-Methode `SecurityService.authenticateAndGetRoles` (`findByPrincipalIdUnrestricted` + `PasswordsUtil`-Hash, konstante Zeit + `security_roles`); RS256-JWT-Util (JDK-only) `JwtService`/`JwtClaims`/`JwtException` in `j-lawyer-server-common` + Unit-Test (6/6 grün); `JwtKeyProvider` lädt Schlüssel aus PKCS12; in `EndpointServiceLocator` registriert; `otp`-Feld reserviert. 2FA nur am Login (mit `add-two-factor-auth` koordinieren). **E2E am laufenden Server verifiziert (2026-07-08):** Login `admin/a`→200+Cookie, ohne Token→401, mit Bearer→200, Refresh→200 (Token rotiert), Logout→204 dann Refresh→401, Falschpasswort→401 ohne `WWW-Authenticate`.
  - [~] 2.3c Client: Login-Route, `AuthService` (Signal-State), `HttpInterceptor` (Bearer + 401-Handling), Route-Guard; Access-Token nur im Speicher — **implementiert gegen Mock-Backend & verifiziert**: `LoginComponent` (Reactive-Form, i18n, hell/dunkel, Demo-Hinweis admin/a), `AuthBackend` (Mock, `sessionStorage`-Refresh-Marker als Platzhalter fürs httpOnly-Cookie), `AuthService` (Signal-Session, Token nur in-memory, `restore()`), funktionaler `authInterceptor` (Bearer nur an API-URLs, 401→Logout), `authGuard` (`canActivateChild`, Redirect `/login?returnUrl`), Shell als bewachte Layout-Route + `/login` außerhalb, `provideAppInitializer` für Silent-Restore, Logout + Nutzer-Initialen im Header. Verifiziert per Headless-Chromium. **Mock durch echte REST-Aufrufe ersetzt & E2E gegen laufenden Server verifiziert (2026-07-08):** `AuthBackend` nutzt `HttpClient` gegen `/j-lawyer-io/rest/v8/auth/*` (`withCredentials`), gemeinsame `core/api.ts`-Basis, Interceptor schließt `/auth/*` von Bearer/401-Logout aus. Browser-Trace: `login→200`, Reload→`refresh→200` (Silent-Restore via httpOnly-Cookie), `logout→204`, Falsch-Login→`401`+Fehleranzeige; Avatar-Initialen „AD" vom echten Server-`username`.
  - [~] 2.3d Verifizieren: Basic-REST-Client und Swing-Client weiterhin unverändert funktionsfähig (Kompatibilität) — **Basic-REST verifiziert (2026-07-08):** `curl -u admin:a /v1/cases/list/active` → 200 unverändert neben aktivem Bearer. **Offen: Swing-Client (EJB-Remoting) gegengeprüft**
  - [ ] 2.3e Härtung: JWT-Keystore **pro Installation** idempotent erzeugen (Docker-Entrypoint/`run.sh` + Installer), niemals einen gemeinsamen Signierschlüssel ausliefern — sonst könnten mit dem installer-eigenen Privatschlüssel gültige Tokens für **jede** Installation gefälscht werden. Bei vorhandenem Keystore unangetastet lassen (Upgrade/Neustart); zusätzlich pro Installation eigenes Store-Passwort (`-Djlawyer.jwt.keystore.password` + `credential-reference`) statt `changeit`. Analog zu WildFlys `generate-self-signed-certificate-host` für TLS. Siehe `j-lawyer-io/AUTH-SETUP.md`.
- [~] 2.4 Fehlende Basis-REST-Endpunkte additiv in neuer API-Version ergänzen (ohne bestehende Versionen zu brechen) — **Cases-Liste erledigt & E2E verifiziert (2026-07-08):** neuer `CasesEndpointV8` (`GET /v8/cases/list` + `/list/active`) mit reicherem `RestfulCaseOverviewV8` (id, fileNumber, name, reason, **subjectField, lawyer, assistant, claimValue, archived**, dateChanged) via `fromArchiveFile`; in `EndpointServiceLocator` registriert. v1 `/list` unverändert (verifiziert: gleiche Keys/Count). Client nutzt `/v8/cases/list`, zeigt Spalte `subjectField · lawyer` + Status-Pill und Filter Alle/Offen/Abgeschlossen (Browser: 804/765/39, deckt sich mit Server). **Serverseitige Filterung/Paginierung/Suche erledigt & E2E verifiziert (2026-07-08):** neuer `GET /v8/cases/page?offset&limit&filter&q` liefert `RestfulCasePageV8` (`total`, `offset`, `limit`, `items`); JPA-Paginierung in `ArchiveFileBeanFacade.findOverviewPage`/`countOverview` (ACL via `SecurityUtils.getAllowedCasesForUser`, `id IN :ids`, optional `archived`, `LIKE` über name/Aktenzeichen/reason/subject/lawyer, `ORDER BY dateChanged DESC`, `limit` 1..200) über `ArchiveFileService.getManagedCasesPage`/`countManagedCases`. `CasesService` lädt seitenweise (`reload`/`loadMore`, `PAGE_SIZE=50`, `reqSeq`-Stale-Guard, `hasMore`), `AktenComponent` mit Infinite-Scroll, serverseitigem Filter-Chip + debounced Suche (250 ms) und echtem `total()`-Badge; client-seitiges `filtered()` entfernt. Browser-Trace: initial 50/804, Filter „Abgeschlossen"→39, Suche „Petzke"→3, Scroll 50→200 via `offset=50/100/150`. **Kontakte analog erledigt (2026-07-08): `GET /v8/contacts/page` — s. Task 3.3.**

## 3. Phase 1 — MVP (Read-first)

- [x] 3.1 Fallliste + Suche + Falldetail — **an echte REST-API angebunden & E2E verifiziert (2026-07-08)**: `AktenComponent` (responsives Master-Detail), durchsuchbare Liste, Detail mit Reiter „Übersicht" (Beteiligte, Fristen, Dokumente, Notiz), i18n (DE/EN), Logo-Tokens, hell/dunkel, Lade-/Fehler-/Retry-Zustände. `CasesService` nutzt `HttpClient`: Liste via `GET /v1/cases/list/active`, Detail lazy via `forkJoin(GET /{id} + /parties + /duedates + /documents)`; Modelle an reale DTOs angeglichen (Beteiligten-Rolle als freier Server-String, Java-`ZonedDateTime`-`[UTC]`-Suffix beim Parsen entfernt, Status client-seitig aus `archived`+Fristen abgeleitet, Bytes/Extension formatiert). Browser-Trace gegen laufenden Server: 765 echte Akten geladen, Detail-Calls alle 200, Suche „Petzke"→3 Treffer, Beteiligte/Fristen real (Rolle „Sachverständiger", Datum 25.05.). **Deep-Linking ergänzt & E2E verifiziert (2026-07-09):** teilbare Route `cases/:id` (vor den Modul-Routen), Route-Param ist Single Source of Truth — `AktenComponent` liest den `:id` per `paramMap`-Subscription und selektiert (auch Akten außerhalb der geladenen Seite via `loadDetail`), Zeilen-Klick navigiert (`open(id)`), Auto-Select navigiert mit `replaceUrl` (keine History-Verschmutzung), `clearSelection`→`/cases`. Browser-Trace: `/cases`→Redirect auf erste Akte, direkter Deep-Link `/cases/654b…`→„LIDL", Zeilen-Klick ändert URL+Detail, Browser-Zurück kehrt korrekt zurück. **Alle Aktendetail-Reiter umgesetzt & E2E verifiziert (2026-07-09):** die vier bisherigen Platzhalter-Reiter sind ausgefüllt — **Beteiligte** & **Fristen** aus den bereits im Detail-`forkJoin` geladenen Daten (kein Serveraufruf; volle Liste mit Avatar/Rolle bzw. Datum/Zuständiger/Typ, erledigte Fristen durchgestrichen+Haken); **Finanzen** aus bestehenden Endpunkten `GET /v7/cases/{id}/invoices` + `GET /v8/cases/{id}/payments` (KPI-Leiste Streitwert/Rechnungen-brutto/Zahlungen + Rechnungs-/Zahlungslisten) — keine Serveränderung nötig; **Historie** über neuen `GET /v8/cases/{id}/history` (`RestfulCaseHistoryV8`, ACL-geprüft via `getHistoryForArchiveFile`, neueste zuerst) als Timeline. Reiter laden lazy beim ersten Öffnen (Historie/Finanzen) mit Lade-/Fehler-/Retry-Zustand und Stale-Guard je Akte. v7-Invoice-Datumsfelder kommen als ISO-String mit `[UTC]`-Suffix (nicht Epoch-ms) → client-seitig via `isoDate()` bereinigt; Währung „€". Browser-Trace (Akte 18/00003 „LIDL"): Beteiligte 5, Fristen 2, Finanzen 4 Rechnungen (brutto-Summe 6.069 €, Fälligkeit 20.01.2026), Historie 626 Einträge, **0 CSP-Verstöße**. **Follow-up: bei sehr großen Listen Virtual-Scrolling; `mine`/Status-Filter serverseitig (Listen-DTO trägt keinen Status); Scroll-in-View auf die deep-verlinkte Zeile; Historie serverseitig paginieren (aktuell alle Einträge).**
- [x] 3.2 Dokumentenliste + Download + Vorschau (PDF nativ) — **im Aktendetail umgesetzt (2026-07-08)**: neuer Reiter „Dokumente" listet alle Dokumente der Akte (`GET /v1/cases/{id}/documents`, bereits im Detail-`forkJoin` geladen) mit Ext-Badge, Datum·Größe und Aktionen. **Download** aller Typen: `GET /v1/cases/document/{id}/content` (Base64-JSON `RestfulDocumentContentV1`) → `base64ToBytes` → Blob → `<a download>`. **Inline-Vorschau** (Overlay-Viewer) CSP-konform: Bilder via `data:`-URL (`img-src 'self' data:`), Text via `<pre>` (UTF-8-Decode). **PDF nativ eingebettet** im Overlay-Viewer via `<iframe [src]=blob:>` (nativer Browser-PDF-Viewer). Dafür CSP gezielt um `frame-src 'self' blob:` erweitert (`index.html`-Meta — der einzige aktuell erzwungene CSP; **kein** Undertow-Header gesetzt), da `'self'` `blob:` nicht matcht; `object-src 'none'` bleibt. Angular: `bypassSecurityTrustResourceUrl` fürs iframe-`[src]` (RESOURCE_URL-Kontext). „Im neuen Tab öffnen" als sekundäre Aktion erhalten. Blob-URL-Lifecycle: revoke bei close/DestroyRef. Nicht darstellbare Typen (docx, xlsx …) nur Download. Keine Serveränderung (bestehende v1-Endpunkte). Neue Icons `download`. **Verifikation:** Server-Endpunkte (Liste + Content) via curl für PDF/JPEG/TXT/DOCX ok; **Client-Decode-/Preview-Logik** (`base64ToBytes`/`previewKindOf`/`mimeOf`) gegen echte Serverdaten in Node geprüft (PDF-Bytes `%PDF`, JPEG-Magic `FF D8 FF`, Text-Decode, DOCX→download-only); Build type-checked; deploytes Bundle enthält Handler+i18n+Icon. **In-Browser-E2E verifiziert (2026-07-08):** Akte 18/00003, Reiter Dokumente (115 Dok.), PDF-Vorschau → eingebettetes `<iframe>` mit `blob:`-src rendert nativen PDF-Viewer, **0 CSP-Verstöße**; Bild-Vorschau → `data:image/`-`<img>`; served Meta-CSP trägt `frame-src 'self' blob:`. **Follow-up: Office-Vorschau via nicht-persistierender Server-Konvertierung (StirlingPdfAPI in-memory), Ordnerstruktur, Papierkorb; autoritativen Undertow-CSP-Header einführen (muss `frame-src 'self' blob:` enthalten).**
- [x] 3.3 Kontakte (Liste/Detail/Suche) — **an echte REST-API angebunden & E2E verifiziert (2026-07-08)**: neuer server-paginierter `GET /v8/contacts/page?offset&limit&filter&q` liefert `RestfulContactPageV8` (`total`, `offset`, `limit`, `items` mit `RestfulContactOverviewV8`: title, salutation, firstName, name, company, department, street(+number), zip, city, country, phone, mobile, email, website via `fromAddressBean`); JPA-Paginierung in `AddressBeanFacade.findOverviewPage`/`countOverview` (Adressbuch ist global, **keine ACL**; `kind`-Filter people=ohne Firma / companies=mit Firma, `LIKE` über name/firstName/company/city/zip/email, `ORDER BY name, company, firstName, limit` 1..200) über `AddressService.getContactsPage`/`countContacts`; in `EndpointServiceLocator` registriert. `KontakteComponent` (responsives Master-Detail, mirror von Akten): server-seitiger Filter Alle/Personen/Firmen, debounced Suche (250 ms), Infinite-Scroll, echtes `total()`-Badge, Avatar-Initialen + Typ-Pill; Detail lazy via bestehendes `GET /v1/contacts/{id}` mit Karten Kontaktkanäle/Anschrift/Weiteres (nur nicht-leere Felder, `tel:`/`mailto:`-Links). Neue Icons `plus/phone/globe/pin`. Browser-Trace gegen laufenden Server: 28181 Kontakte, Filter Firmen→5242 (alle Typ „Firma") / Personen→22939 (alle „Person"), Summe = Alle; Suche „meier"→45, Scroll 50→200 via `offset=50/100/150`, Detail-Klick lädt `GET /v1/contacts/{id}`.
- [x] 3.4 Kalender-/Wiedervorlagen-Ansicht — **an echte REST-API angebunden & E2E verifiziert (2026-07-08)**: neuer `GET /v8/calendar/events?from&to&type&status&limit` liefert `RestfulCalendarEventV8[]` (id, type followup/respite/event, summary, description, location, begin/end als Epoch-ms, done, assignee, reminderMinutes, caseId/caseFileNumber/caseName via `fromReview`); nutzt bestehende **ACL-restringierte** `CalendarService.searchReviews(status, type, from, to, limit)` (Datum `yyyy-MM-dd` server-TZ; type all/followup(10)/respite(20)/event(30), status all/open(0)/done(1)), in `EndpointServiceLocator` registriert. Keine EJB-Änderung. `KalenderComponent` (Agenda, nach Tag gruppiert): Monatsnavigation ‹/›/Heute, Typ-Filter-Chips (Alle/Termine/Fristen/Wiedervorlagen), „Nur offene"-Toggle, echtes `count`-Badge; locale-bewusste Labels via `Intl.DateTimeFormat(activeLang)`; Termine mit Uhrzeit, Fristen/WV ganztägig; done-Einträge durchgestrichen/gedimmt + grüner Haken; Typ-Farbleiste + Pill (blau/rot/amber); Akten-Bezug + Zuständiger. Neue Icons `check`. Browser-Trace gegen laufenden Server: Juli 2026 → 4 WV (nach Tag gruppiert, „Heute"-Badge), Filter Fristen→0 (`?type=respite`), Monatswechsel→August (`?from=2026-08-01`), Heute→Juli, „Nur offene"→`?status=open`; April 2026 → 37 Einträge mit allen drei Typen (Termin/Frist/WV) + Uhrzeiten. **Klick auf Eintrag → Aktendetail (Deep-Link) erledigt & E2E verifiziert (2026-07-09):** Kalendereintrag mit Aktenbezug ist ein `[routerLink]="['/cases', caseId]"` (Einträge ohne `caseId` bleiben nicht-verlinkter Text); Browser-Trace: Klick auf Juli-WV „52/2026 · Becker…" navigiert zu `/cases/1536f5eb…` und lädt das Aktendetail. **Follow-up (Phase 2): Monatsraster-Ansicht, Anlegen/Bearbeiten von Terminen/Fristen.**
- [ ] 3.5 Office-Bearbeitung via Download → lokal bearbeiten → Re-Upload (Fallback)
- [~] 3.6 „Mein Desktop" (Dashboard) — **erste Version umgesetzt & E2E verifiziert (2026-07-09):** das bisherige Platzhalter-Modul `desktop` ist jetzt ein Read-only-Dashboard, das die wichtigsten Widgets des Swing-`DesktopPanel` spiegelt, **ausschließlich aus bestehenden Endpunkten zusammengesetzt (keine Serveränderung):** **Zuletzt geändert** (`GET /v8/cases/page?limit=8`, bereits nach `dateChanged DESC` sortiert), **Fällig & Wiedervorlagen** (`GET /v8/calendar/events?status=open`, Fenster −14…+30 Tage, `!done`, soonest-first, überfällige rot markiert), **Offene Rechnungen** (`GET /v7/cases/invoices` → KPI Anzahl + Summe brutto + Top-6). `DesktopComponent`/`DashboardService` (drei unabhängige Signale mit Lade-/Fehler-/Retry-Zustand), Begrüßung mit `AuthService.user().displayName`, jede Zeile Deep-Link auf `/cases/:id`, „Alle ›"-Links in die Module. Browser-Trace: 8 Akten, 9 fällige Einträge (7 überfällig), 23 offene Rechnungen (Summe 1.711.132,32 €, inkl. Gutschrift −312,38 €), Klick auf Akte öffnet Detail, **0 CSP-Verstöße**. Nicht abgebildete Swing-Widgets: **Etiketten/Tagged** (braucht Tag-Endpunkte), **Messenger** (messagestome/-toothers). **Als Default-Landing-Route gesetzt (2026-07-09):** `''` und `**` redirecten auf `desktop`, Login-`returnUrl`-Default `/desktop`; Browser verifiziert (Login→`/desktop`, Root→`/desktop`, unbekannt→`/desktop`, Deep-Links/`/cases`-Nav unberührt). **Follow-up: Tagged- & Messenger-Widget; konfigurierbares Grid/Layout wie im Desktop (`DesktopLayoutPreset`).**

## 4. Phase 2 — Kernmodule (Write)

- [x] 4.1 Fälle/Dokumente/Kontakte/Kalender voll editierbar (inkl. Beteiligte, Notizen, Fristen) — **Update (2026-07-16): Schreibstrecken im Kern vollständig** — Fälle (`case-editor`, Stammdaten) + Beteiligte (`party-editor`/`party-add`) editierbar (2026-07-14), Kontakte editierbar (`contact-editor`, 2026-07-13), Dokumente einzeln + als Stapel bearbeitbar (`document-actions`/`document-bulk-bar`, 2026-07-14/15), Kalender aus allen Ansichten bearbeit-/löschbar inkl. Drag-Reschedule (2026-07-14). Details der einzelnen Strecken siehe 4.1a/4.2 und die jeweiligen Follow-ups. **Kalender-CRUD als erste Schreib-Strecke umgesetzt & E2E verifiziert (2026-07-10):** das Kalender-Modul kann Einträge (Termine/Fristen/Wiedervorlagen) jetzt **anlegen, bearbeiten, löschen und als erledigt markieren**. Frontend: `EventEditorComponent` (modaler Dialog, standalone/Signals, CSP-konform) — Eintragstyp folgt dem gewählten Kalender (`eventType` aus `GET /v4/calendars/list`, nur `EVENT`-Kalender sind terminiert/zeitgebunden), Akten-Auswahl beim Anlegen via debounced Suche (`GET /v8/cases/page`), beim Bearbeiten fix; `CalendarService` um `loadCalendars`/`searchCases`/`save`/`remove`/`toggleDone` erweitert (kalte Observables, genau-einmal-Subscribe, `saving`-Signal, Reload nach Erfolg). „＋ Neu"-Button in der Kopfzeile, Erledigt-Umschalter + Bearbeiten-Stift je Agenda-Zeile. **REST-Lücke geschlossen (2026-07-10):** Create/Update existierten bereits (`PUT /v6/cases/duedate/{create,update}`), aber **Delete fehlte** — additiv ergänzt als `DELETE /v8/calendar/events/{id}` in `CalendarEndpointV8` (ACL: lädt Review via `CalendarService.getReview`, prüft Zugriff über `ArchiveFileService.getArchiveFile` auf die Akte, `removeReview`; `getReview`/`removeReview` zusätzlich ins `CalendarServiceLocal`-Interface aufgenommen; `writeArchiveFileRole`); EAR neu gebaut & deployt. **E2E am laufenden Server verifiziert:** Create→200, Update (Umbenennen+erledigt)→200 (Read-back bestätigt Änderung), Delete→204/danach 404, ohne Auth→401. **Follow-up: Bearbeiten/Löschen auch aus Monat-/Wochen-/Tagesansicht (bisher nur Agenda + „Neu" überall); Typwechsel eines bestehenden Eintrags (v6-`update` setzt eventType nicht); Terminkonflikt-Prüfung (`getConflictingEvents`); Kalender-Setup-Verwaltung.**
- [x] 4.1a Fälle/Dokumente/Kontakte editierbar (der Rest von 4.1 außer Kalender) — **umgesetzt (2026-07-13…15):** `case-editor` (Fall-Stammdaten) + `party-editor`/`party-add` (Beteiligte); `contact-editor` (Kontakte anlegen/bearbeiten/löschen, inkl. Kontakt-Dokumente Upload/Löschen); Dokument-Aktionen einzeln (`document-actions`) und als Stapel (`document-bulk-bar`): Umbenennen, Datum, Favorit, Highlight, Verschieben, Etiketten (aus Wertevorrat, als Chips), zu-PDF, OCR, Löschen.
- [~] 4.2 Zeiterfassung — **umgesetzt (2026-07-14):** `timesheet-tracking.service` + `timesheet-detail`/`timesheet-editor`/`timesheet-log` (Projekte, Einträge, Stoppuhr, aktenübergreifendes Protokoll). Offen: erneute Parität/Verifikation gegen den Swing-Client in dieser tasks.md nachziehen.
- [~] 4.3 Volltextsuche (Lucene) mit Trefferliste/Relevanz — **globales Dokumente-Modul (Read) umgesetzt & E2E verifiziert (2026-07-09):** das bisherige Platzhalter-Modul `documents` ist jetzt eine Lucene-Volltextsuche über alle Aktendokumente. Nutzt den bestehenden `GET /v8/search/fulltext?query=&maxDocs` (ACL-serverseitig, `RestfulSearchHitV8`: Dokument-ID, Dateiname, Akten-ID/-Nummer/-Name, Snippet, Score) — **keine Serveränderung**. `DokumenteComponent` (`DocumentsService`): debounced Suche (300 ms), `?q=`-Query-Param als Single Source of Truth (teilbar/bookmarkbar, Back/Forward spielt Suche erneut ab), Treffer mit Ext-Badge, Score-Pill, Snippet mit **CSP-sicherer Query-Hervorhebung** (Segmentliste + `<mark>`, kein innerHTML), Akten-Bezug als Deep-Link `/cases/:archiveFileId` (nutzt das neue Deep-Linking) und Direkt-Download (Wiederverwendung `documentContent`). Ergebniskappung bei `maxDocs=50` mit Hinweis. Browser-Trace: Suche „Mandant" → `?q=Mandant`, 50 Treffer (Badge „50+"), Snippet-Highlights, Klick auf „18/00003 · LIDL" öffnet Aktendetail „LIDL", **0 CSP-Verstöße**. **Feinschliff umgesetzt (2026-07-09):** (1) **Preview-Overlay als shared Component extrahiert** — `shared/document-preview.component.ts` (`<jl-document-preview [doc] (closed)>`, Input `PreviewDoc {id,name,ext}`) + `shared/document-preview.models.ts` (`previewKindOf`/`mimeOf`/`base64ToBytes`/`bytesToText`, `DocPreviewKind`) + `shared/document-content.service.ts` (`content()`/`download()`); Aktendetail (Reiter Dokumente) und Suche teilen sich jetzt genau eine Vorschau-/Download-Implementierung (Duplikate + `cases.documentContent` entfernt). (2) **Inline-Vorschau im Suchergebnis**: Klick auf einen darstellbaren Treffer bzw. „Vorschau"-Button öffnet das Overlay (PDF/Bild/Text), sonst Download. (3) **Dokumentnamen-Umschalter** „Nur Dateinamen durchsuchen" (`?fn=1`, client-seitige Filterung der Treffer per Dateiname; blendet Snippets aus). (4) **Globale Header-Suche verdrahtet**: Enter im Kopfzeilen-Suchfeld navigiert zu `/documents?q=…`. Neue i18n-Keys `preview.*` + `dokumente.{preview,fileNameOnly,emptyFileName}`. Reine Client-Änderung; `ng build --configuration production` type-checked & gebaut. **Follow-up: admin-only Reindex-Aktion (`POST /v8/search/reindex`), Paginierung über `maxDocs=50` hinaus.**
- [~] 4.4 E-Mail-Client (IMAP/SMTP) inkl. Vorlagen — **Lesen & Triage umgesetzt & E2E verifiziert (2026-07-09…16):** Postfach-/Ordnerbaum, Nachrichtenliste mit Infinite-Scroll + Alle/Ungelesen-Filter + optionalem Zeitraum + Suche, Reader (CSP-sicheres Blob-iframe, Inline-cid-Bilder), Aktionen Gelesen/Ungelesen, Verschieben, Löschen, Anhang-/EML-Download; **Ordner ausblenden** + verzögerte Zähler für schnelleres initiales Rendern (Server-API in der archivierten Change `extend-email-rest-api-folders-and-paging` dokumentiert). **Verfassen/Senden umgesetzt (2026-07-16):** `EmailComposeComponent` (modaler Composer) — Neu/Antworten/Allen-antworten/Weiterleiten, Von-Auswahl (mehrere Postfächer), An/Kopie/Blindkopie mit Adressbuch-Autovervollständigung (debounced Typeahead gegen `GET /v8/contacts/page`), Betreff, Klartext-Body mit zitiertem Original + Threading-Headern (`inReplyTo`/`references`), Datei-Anhänge (Base64) inkl. Übernahme der Original-Anhänge beim Weiterleiten; Versand via bestehendem `POST /v7/email/mailboxes/{id}/send` (kopiert serverseitig in „Gesendet", keine Serveränderung). **Akten-Vorschläge im Reader (2026-07-16):** serverseitiger `POST /v7/email/mailboxes/{id}/case-suggestions` reproduziert die Swing-Heuristik (Aktenzeichen/Fremdreferenz in Betreff/Body, Absender→Kontakte→Akten, Telefonnummern-Extraktion), ACL-gefiltert; Reader zeigt passende Akten als Deep-Links + Rufnummern (archivierte Change `add-email-case-suggestions`). **Offen: HTML-Body/Rich-Text-Editor + Vorlagen; „E-Mail an Akte speichern"-Aktion.**

## 5. Phase 3 — Finanzen & Kommunikation

- [~] 5.1 Rechnungen/Zahlungen inkl. ZUGFeRD/XRechnung, Girocode/EPC-QR — **Rechnungen & Zahlungen editierbar umgesetzt (2026-07-15):** `invoice-editor`/`invoice-positions`/`position-editor` (Rechnung anlegen/bearbeiten/löschen/duplizieren/als-bezahlt, Positionen), `payment-editor` (Zahlungen anlegen/bearbeiten/löschen, servergenerierte Nummer bleibt erhalten), `account-entry-editor`; Finanz-Reiter im Aktendetail. **Offen: ZUGFeRD/XRechnung-Erzeugung, Girocode/EPC-QR.**
- [ ] 5.2 Messenger mit serverseitigem Push
- [~] 5.3 beA-Postfach — **Modul umgesetzt (2026-07-09):** `bea`-Modul (`bea.component`/`bea.service`) für beA-Nachrichten (Lesen/Triage analog E-Mail, eigenes Restriction-Preset). Offen: Umfang (Senden/EEB) + Verifikation nachziehen.
- [~] 5.4 Reporting (Tabellen/Diagramme) — **„Auswertungen"-Modul umgesetzt (2026-07-09):** `reporting.component`/`reporting.service`. Offen: Diagramm-/Auswertungsumfang + Parität nachziehen.

## 6. Phase 4 — Parität & Komfort

- [ ] 6.1 In-Browser-Office-Editing via WOPI (Collabora/OnlyOffice)
- [ ] 6.2 Serienschreiben (Mass Mail)
- [ ] 6.3 KI-Assistent (Ingo) inkl. Tool-Calling/Approval
- [ ] 6.4 Administration/Einstellungen (Nutzer, Rollen, Systemkonfiguration)
- [ ] 6.5 VoIP/Fax/E-POST (Click-to-Dial, Statusverfolgung)
- [ ] 6.6 Datei-Upload-Ersatz für Scanner-Workflow (inkl. Mobile-Kamera-Upload)

## 7. Übergreifend

- [ ] 7.1 Barrierefreiheit (WCAG/BITV) und i18n (DE primär) durchgängig prüfen
- [ ] 7.2 Visual-Regression-/Lint-Gates für Design-Konsistenz in CI — über alle Breakpoints (Desktop/Tablet/Smartphone)
- [ ] 7.4 Responsive/Touch-Tests auf realen Geräteklassen; optional PWA-Ausbaustufe evaluieren
- [ ] 7.3 Dokumentation: bewusst nicht abgebildete Desktop-Funktionen (Scanner/TWAIN, Diktierhardware, Ordner-Watch)
